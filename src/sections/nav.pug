mixin nav(className, items, hrefItems)
  nav(class=`${className}`)
    ul(class=`${className}__list`)
      each item, ndx in items
        li(class=`${className}__item`)
          a(class=`${className}__link` href='#')=item
          //- ${hrefItems[ndx]}